query ListBusinessAccountHolders($first: Int, $after: String) {
  businessAccountHolders(first: $first, after: $after) {
    edges {
      node {
        ...BusinessAccountHolderSummary
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
